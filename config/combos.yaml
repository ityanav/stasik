# Combo sets — shared between instances
# Лев 🦁 — прибыльные комбинации (прямой вход)
lev:
  - [cluster, fib, fvg]
  - [cluster, fib, ob, sweep]
  - [fvg, rsi_div]
  - [cluster, fib, vol]
  - [cluster, fib, fvg, sweep]
  - [cluster, fib, sweep, vol]
  - [cd, cluster, fib, sweep, vol]
  - [cluster, fib, ote, sweep, vol]

# Шакал 🐺 — убыточные комбинации (вход в обратную сторону)
shakal:
  - [cd, cluster, fib, mom, ote]
  - [cluster, fib, fvg, mm, sweep]
  - [cluster, fib, ote, sweep]
  - [cluster, fib, ob]
  - [cluster, fib, sweep]
  - [cd, cluster, fib, sweep]
  - [cd, cluster, fib, ote, sweep]
  - [fib, fvg, sweep]
