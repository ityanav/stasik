instance_name: "TURTLE"
initial_balance: 164235

database:
  path: "/root/stasik/data/turtle.db"

bybit:
  testnet: false
  demo: true
  api_key: "Ct24OvPFimR3vLSlcW"
  api_secret: "CNzK2w9eQ41j4A4dszBLvvOkAH61Z5BQG9oq"

trading:
  pairs: ["BTCUSDT", "ETHUSDT", "SOLUSDT", "XRPUSDT", "ADAUSDT", "AVAXUSDT", "LINKUSDT", "NEARUSDT", "SUIUSDT"]
  market_type: "futures"
  timeframe: "240"           # 4H candles
  htf_timeframe: "D"         # Daily for HTF trend
  leverage: 2
  trading_hours: [0, 24]     # 24/7 crypto
  tick_interval: 300          # check every 5 min (pyramid/exit checks)

strategy:
  strategy_mode: "turtle"
  min_score: 1                # 1 breakout is enough
  ema_fast: 9
  ema_slow: 21
  adx_min: 0                 # disabled — Turtle WANTS trends
  adx_max: 0
  reversal_confirmation: false  # breakout = instant entry
  market_bias: false
  fng_extreme_greed: 100
  fng_extreme_fear: 0
  funding_rate_max: 999
  htf_filter: false
  patterns_enabled: false
  orderbook_enabled: false

turtle:
  entry_period_s1: 20         # System 1 entry channel
  exit_period_s1: 10          # System 1 exit channel
  entry_period_s2: 55         # System 2 entry channel
  exit_period_s2: 20          # System 2 exit channel
  n_period: 20                # ATR period for N
  risk_pct: 1.0               # 1% equity per unit
  max_units: 4                # max 4 units per market
  pyramid_n_mult: 0.5         # add every 0.5N
  sl_n_mult: 2.0              # SL = 2N

risk:
  risk_per_trade: 1.0
  stop_loss: 5.0              # safety net (2N should hit first)
  take_profit: 20.0           # safety net (exit channel should hit first)
  max_open_positions: 40      # 4 units x 9 pairs = 36 max
  max_daily_loss: 15.0
  cooldown_after_loss: 0      # no cooldown
  min_tp_net: 0               # no min TP (exit by channel)
  commission_rate: 0.055
  atr_period: 20
  atr_sl_multiplier: 2.0
  atr_tp_multiplier: 4.0
  atr_trailing_multiplier: 1.5
  atr_sl_min: 0.5
  atr_sl_max: 10.0
  atr_tp_min: 1.0
  atr_tp_max: 20.0
  atr_trail_min: 0.5
  atr_trail_max: 5.0
  trailing_stop: 0
  trailing_activation: 0
  breakeven_activation: 0
  smart_exit_min: 0
  partial_close_enabled: false
  max_per_group: 2
  correlation_groups:
    majors: ["BTCUSDT", "ETHUSDT"]
    alt_l1: ["SOLUSDT", "AVAXUSDT", "NEARUSDT", "SUIUSDT"]
    alt_infra: ["XRPUSDT", "ADAUSDT", "LINKUSDT"]

ai:
  enabled: false

dashboard:
  enabled: false

telegram:
  token: "7852966698:AAFA_L6ovqXtTmMO05DBZg3UYbx4DklIY7k"
  chat_id: "885025581"
  notify_only: true
